# RNL Guardian — Discord Moderation Bot

Quick start (Windows):
1) Install Python 3.12+
2) Duplicate `.env.example` -> rename to `.env` and paste your token.
3) In this folder run:
   pip install -r requirements.txt
   python bot.py
